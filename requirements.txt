Flask==3.0.3
Flask-SQLAlchemy==3.1.1
python-dotenv==1.0.1
SQLAlchemy==2.0.35
Werkzeug==3.0.4
reportlab==4.2.5
qrcode==7.4.2
Pillow==10.4.0
email-validator==2.2.0

# Testing (dev dependency)
pytest==8.3.3

# NOTE: Microsoft SQL Server support (pyodbc) is NOT installed by this file.
# pyodbc requires either a prebuilt wheel for your exact Python version/OS,
# or Microsoft C++ Build Tools to compile from source. Since it's only
# needed when DB_ENGINE=mssql, install it separately:
#   pip install -r requirements-sqlserver.txt
# See README.md > Troubleshooting for details.
